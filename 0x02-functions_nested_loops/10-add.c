#include "main.h"
/**
 *add - a program that adds two integers.
 *Return: 0.
 *@n: parameter for one integer
 *@c: parameter for the other integer.
 */
int add(int n, int c)
{
	int result;

	result = n + c;
	return (result);
}
