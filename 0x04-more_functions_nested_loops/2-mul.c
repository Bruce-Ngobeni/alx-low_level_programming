#include "main.h"
/**
 *mul - a function that multiplies two integers
 *@a: input parameter for first integer.
 *@b: input parameter for second integer.
 *Return: 1 (success).
 */

int mul(int a, int b)
{
	return (a * b);
}
