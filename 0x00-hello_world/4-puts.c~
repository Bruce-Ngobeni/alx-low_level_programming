#include <stdio.h>
/**
 * main - Writing a program that prints a strings using puts function.
n
 * Return: 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
