/**
 * set_string - a function that sets the value of a pointer to a char.
 * @s: a pointer pointing a location of a string.
 * @to: a poniter pointing a location of a string.
 *
 * Return: nothing.
 */
void set_string(char **s, char *to)
{
	*s = to;
}
